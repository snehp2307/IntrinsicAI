# Core
flask>=3.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11
werkzeug>=3.0

# Scheduler — auto-retrain + monthly reset
apscheduler>=3.10

# Payment processing
stripe>=7.0

# XAI libraries (install for exact values; approximations work without these)
shap>=0.44
lime>=0.2
dice-ml>=0.11

# Optional extras
gunicorn>=21.0          # production WSGI server
python-dotenv>=1.0      # load .env file for secrets